[package]
name = "my-c-project-rs"
version = "0.1.0"
edition = "2021"

# 根据迁移过程中的需要添加依赖
[dependencies]
# lazy_static = "1.4"  # 取消注释如果需要全局状态
# bitflags = "2"        # 取消注释如果需要位标志
# libc = "0.2"          # 取消注释如果需要 C 类型兼容
